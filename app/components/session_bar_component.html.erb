<div class="session_bar_component">
  <% if authenticated? %>
    <div class="background">
      <%= render LinkComponent.new(link_text: "管理画面", path: controls_path, color: "white") %>
      <%= render LinkComponent.new(link_text: "ログアウト", path: session_path, color: "white") %>
    </div>
  <% end %>
</div>
